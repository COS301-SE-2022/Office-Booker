import { DrawableDirective } from './drawable.directive';

describe('DrawableDirective', () => {
  it('should create an instance', () => {
    const directive = new DrawableDirective();
    expect(directive).toBeTruthy();
  });
});
