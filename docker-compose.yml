version: '3'
services:
  mongodb:
    image: mongo:latest
    volumes:
      - db-data:/data/db
      - mongo-config:/data/configdb
    ports:
      - 27017:27017
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: password

volumes:
  db-data:
  mongo-config:
